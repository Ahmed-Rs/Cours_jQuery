$(document).ready(function(){      // On selectionne le fichier avec "document, ici, ready est l'évènement", et ensuite il ya la fct. dans les parenthèses du ready.

    $(".par1").hide();             // On selectionne le selector class .par1.
});